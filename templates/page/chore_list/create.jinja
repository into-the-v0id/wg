{% extends "layout/default.jinja" %}

{% block title %}Create 📋&nbsp;Chore List{% endblock %}

{% block back_link %}
    <a href="/chore-lists" class="secondary">&larr; Back</a>
{% endblock %}

{% block content %}
    <form method="post">
        <label for="name">Name</label>
        <input id="name" name="name" type="text" required />

        <label for="description">
            Description
            <i style="opacity: 0.6;">(optional)</i>
        </label>
        <textarea id="description" name="description"></textarea>

        <label for="score_reset_interval">Score Reset Interval</label>
        <select id="score_reset_interval" name="score_reset_interval" required>
            <option selected disabled hidden value=""></option>
            {% for score_reset_interval in score_reset_intervals %}
                <option value="{{ score_reset_interval }}">{{ score_reset_interval }}</option>
            {% endfor %}
        </select>

        <button type="submit">Create</button>
    </form>
{% endblock %}
